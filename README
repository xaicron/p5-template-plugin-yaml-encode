This is Perl module Template::Plugin::YAML::Encode.

INSTALLATION

Template::Plugin::YAML::Encode installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan Template::Plugin::YAML::Encode

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Template::Plugin::YAML::Encode documentation is available as in POD. So you can do:

    % perldoc Template::Plugin::YAML::Encode

to read the documentation online with your favorite pager.

Yuji Shimada
